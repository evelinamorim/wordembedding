
def process_wiki_str(str_w):
    new_str_w = ''
    i = 0
    n = len(str_w)
    while (i<n):
        if ('a' <= str_w[i] and str_w[i]<='z'):
             new_str_w +=str_w[i]
        elif ('A' <= str_w[i] and str_w[i]<='Z'):
             new_str_w +=str_w[i].lower()
        elif ('0' <= str_w[i] and str_w[i]<='9')
             new_str_w +=str_w[i]
        elif ()
        i = i+1
